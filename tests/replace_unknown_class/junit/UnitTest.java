import tester.annotations.*;

@Exercises({@Ex(exID = "replace_unknown_class", points = 12.5)})
public class UnitTest {
	@Points(exID = "replace_unknown_class", bonus = 0.1, comment = "empty dummy test")
	public void pubTest_no_op() {
	}
}
