import tester.annotations.*;

@Exercises({@Ex(exID = "import_cleanonly", points = 4)})
public class UnitTest {
	@Points(exID = "import_cleanonly", bonus = 1, comment = "empty dummy test")
	public void test_noOp() {
	}
}
