import tester.annotations.*;

@Exercises({@Ex(exID = "replaced_without_clean", points = 12.5)})
public class UnitTest {
	@Points(exID = "replaced_without_clean", bonus = 0.1, comment = "empty dummy test")
	public void pubTest_no_op() {
	}
}
