import tester.annotations.*;

@Exercises({@Ex(exID = "show_replace_error", points = 12.5)})
public class UnitTest {
	@Points(exID = "show_replace_error", bonus = 0.1, comment = "empty dummy test")
	public void pubTest_no_op() {
	}
}
