import tester.annotations.*;

@Exercises({@Ex(exID = "replace_generic_many_param", points = 2)})
public class PublicTest {
	@Points(exID = "replace_generic_many_param", bonus = 1)
	public void pubTest_no_op() {
	}
}
