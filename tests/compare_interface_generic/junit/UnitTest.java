import tester.annotations.*;

@Exercises({@Ex(exID = "compare_interface_generic", points = 1.0)})
@CompareInterface({"ToTest"})
public class UnitTest {
	@Points(exID = "compare_interface_generic", bonus = 1.0)
	public void testEmpty() {
	}
}
